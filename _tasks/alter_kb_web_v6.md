# Changes to kb-web (v6)

## Similar articles similarity score minimum

I want to filter the smillar results displayed for articles by similarity score; setting a minimum as .8 or higher for articles to be displayed.

## Segmentation for Youtube video transcripts

I want to segment the youtube videos into their own section on the website, filtering the videos by creator and displaying an iframe of the youtube videos on their article pages. I want the wiki generation for the articles to be generated by an agent that has specific instructions for interpreting and segmenting youtube transcripts into wiki articles (with titles and tags like the articles). WIkis generated for youtube content that is ingested will be like a normal wiki but include a video breakdown with specific quoted points for wach section from the transtript with timestamps.

## Rendering issues on wiki pages

On some if not most of the wiki entries that the agent is creating they are using '*' to denote sub-list items but the renderer in the html tmeplate is not rendering the quoted text as a list instead just mashing the text together without bullet points. If we could fix this somehow; I know that the * character is used in markdown for other purposes such as making text bold or italic. It would be ideal to isolate singular use-cases of * in the generated text to properly render the wiki pages.

## MCP Server configuration file

I want to update the kb-web MCP server and provide a service configuration for the MCP server depolyment (like the current configuration for kb-web)
